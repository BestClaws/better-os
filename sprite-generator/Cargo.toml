[package]
name = "sprite-generator"
version = "0.1.0"
edition = "2021"
default-run = "sprite-generator"

[dependencies]
rust-gfx = { path = "../rust-gfx", default-features = false, features = ["std"] }
