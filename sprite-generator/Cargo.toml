[package]
name = "sprite-generator"
version = "0.1.0"
edition = "2021"

[dependencies]
gfx = { path = "../gfx" }
math = { path = "../math" }
bmp = "0.5"
zeno = { version = "0.3.3", default-features = false, features = ["libm", "eval"] }
